name: wt_firepod_auth
description: "A new Flutter package project."
version: 0.0.1
publish_to: 'none'
homepage: https://github.com/tmcmaster/wt_firepod_auth

environment:
  sdk: '>=3.2.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  firebase_core: 
  flutter:
    sdk: flutter
  flutter_riverpod: 
  font_awesome_flutter: 
  google_sign_in:
  wt_app_scaffold:
    git:
      url: https://github.com/tmcmaster/wt_app_scaffold.git
  wt_firepod:
    git:
      url: https://github.com/tmcmaster/wt_firepod.git
  wt_logging:
    git:
      url: https://github.com/tmcmaster/wt_logging.git
  wt_models:
    git:
      url: https://github.com/tmcmaster/wt_models.git

dev_dependencies:
  flutter_lints: 
  flutter_test:
    sdk: flutter

